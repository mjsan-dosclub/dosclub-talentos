import Link from 'next/link';import {JoinForm} from './join-form';
export default async function JoinPage({searchParams}:{searchParams:Promise<{token?:string}>}){const {token}=await searchParams;return <main className="access-layout"><section className="access-intro"><Link className="eyebrow" href="/">← DOS CLUB TALENTOS</Link><p className="eyebrow">ACCEPT INVITATION</p><h1>Begin with<br/><em>your journey.</em></h1><p>Your seat is confirmed when this invitation is successfully accepted. Group capacity is first-accepted, first-confirmed.</p></section><JoinForm token={token||''}/></main>}
