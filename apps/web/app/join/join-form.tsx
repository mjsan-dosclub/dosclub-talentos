'use client';import {FormEvent,useState} from 'react';import Link from 'next/link';
export function JoinForm({token}:{token:string}){const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');const[done,setDone]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setMessage('');const data=new FormData(event.currentTarget);const password=String(data.get('password'));if(password!==String(data.get('confirm'))){setMessage('Passwords do not match.');setBusy(false);return}const response=await fetch('/api/invitations/accept',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,password})});const body=await response.json();if(response.ok)setDone(true);else setMessage(body.error||'The invitation could not be accepted.');setBusy(false)}if(!token)return <section className="access-form"><h3>Invitation link missing</h3><p>Open the complete link from your DOS Club invitation email.</p></section>;if(done)return <section className="access-form"><h3>Your account is ready.</h3><p>Your place has been confirmed. You can now sign in to TalentOS.</p><Link className="button" href="/access">Continue to sign in ↗</Link></section>;return <form className="access-form" onSubmit={submit}><div><label htmlFor="password">Create password</label><input id="password" name="password" type="password" autoComplete="new-password" minLength={12} maxLength={128} required/></div><div><label htmlFor="confirm">Confirm password</label><input id="confirm" name="confirm" type="password" autoComplete="new-password" minLength={12} maxLength={128} required/></div><button className="button" disabled={busy}>{busy?'Confirming…':'Accept invitation'} <span aria-hidden="true">↗</span></button><p className="form-help">Use at least 12 characters. Acceptance confirms your account and, for students, the available group seat.</p><p className="form-message" role="status" aria-live="polite">{message}</p></form>}
