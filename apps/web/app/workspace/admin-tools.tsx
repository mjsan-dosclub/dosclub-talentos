'use client';import {FormEvent,useState} from 'react';
type Cohort={id:string;name:string;batch:string;institution_id:string;institution:string;kind:string;capacity:number;enrolled:number};
async function post(url:string,body:unknown){const response=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const result=await response.json();if(!response.ok)throw new Error(result.error||'The request could not be completed');return result}
export function AdminTools({cohorts}:{cohorts:Cohort[]}){const[message,setMessage]=useState('');const[busy,setBusy]=useState('');async function submit(kind:string,event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(kind);setMessage('');const form=new FormData(event.currentTarget);try{if(kind==='cohort')await post('/api/cohorts',{institution:form.get('institution'),kind:form.get('kind'),batch:form.get('batch'),group:form.get('group'),capacity:Number(form.get('capacity')),reason:form.get('reason')});if(kind==='invite')await post('/api/invitations',{email:form.get('email'),name:form.get('name'),role:'student',institutionId:null,groupId:form.get('groupId'),expiresAt:new Date(String(form.get('expiresAt'))).toISOString(),reason:form.get('reason')});if(kind==='session'){const required=form.get('required')==='yes';await post('/api/sessions',{title:form.get('title'),objective:form.get('objective'),groupId:form.get('groupId'),startsAt:new Date(String(form.get('startsAt'))).toISOString(),endsAt:new Date(String(form.get('endsAt'))).toISOString(),mode:form.get('mode'),venue:form.get('venue'),required,types:required?[form.get('evidenceType')]:[],deadline:required?new Date(String(form.get('deadline'))).toISOString():null,reason:form.get('reason')})}setMessage(kind==='invite'?'Invitation sent.':'Saved successfully.');event.currentTarget.reset();if(kind!=='invite')setTimeout(()=>location.reload(),500)}catch(error){setMessage((error as Error).message)}finally{setBusy('')}}return <section className="workspace-section admin-tools"><div className="workspace-section-title"><p className="eyebrow">PROGRAMME TOOLS</p><h2>Prepare the next step.</h2></div><div className="tool-grid"><details><summary>Create cohort</summary><form onSubmit={e=>submit('cohort',e)}><label>Institution<input name="institution" required/></label><label>Programme type<select name="kind"><option>B2B</option><option>B2C</option></select></label><label>Batch<input name="batch" required/></label><label>Group<input name="group" required/></label><label>Capacity<input name="capacity" type="number" min="1" defaultValue="40" required/></label><label>Reason<textarea name="reason" required/></label><button disabled={Boolean(busy)}>Create cohort</button></form></details><details><summary>Schedule session</summary><form onSubmit={e=>submit('session',e)}><label>Workshop title<input name="title" required/></label><label>Learning objective<textarea name="objective" required/></label><label>Group<select name="groupId" required><option value="">Choose group</option>{cohorts.map(c=><option key={c.id} value={c.id}>{c.institution} · {c.name}</option>)}</select></label><label>Starts<input name="startsAt" type="datetime-local" required/></label><label>Ends<input name="endsAt" type="datetime-local" required/></label><label>Mode<select name="mode"><option>Online</option><option>Offline</option><option>Hybrid</option></select></label><label>Venue or meeting reference<input name="venue" required/></label><label>Evidence required?<select name="required"><option value="no">No</option><option value="yes">Yes</option></select></label><label>Evidence type<select name="evidenceType"><option>GitHub Repository</option><option>Project URL</option><option>Reflection</option><option>External Assessment</option></select></label><label>Evidence deadline<input name="deadline" type="datetime-local"/></label><label>Reason<textarea name="reason" required/></label><button disabled={Boolean(busy)}>Schedule session</button></form></details><details><summary>Invite student</summary><form onSubmit={e=>submit('invite',e)}><label>Student name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Group<select name="groupId" required><option value="">Choose group</option>{cohorts.filter(c=>c.enrolled<c.capacity).map(c=><option key={c.id} value={c.id}>{c.institution} · {c.name} · {c.capacity-c.enrolled} confirmed seats open</option>)}</select></label><label>Invitation expires<input name="expiresAt" type="datetime-local" required/></label><label>Reason<textarea name="reason" required/></label><p>Invitations do not reserve capacity. The seat is confirmed when accepted.</p><button disabled={Boolean(busy)}>Send invitation</button></form></details></div><p className="form-message" role="status" aria-live="polite">{message}</p></section>}
